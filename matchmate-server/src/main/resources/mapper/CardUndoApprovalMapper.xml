<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cinoo.matchmateserver.mapper.CardUndoApprovalMapper">

    <select id="selectByRequestIds" resultType="com.cinoo.matchmateserver.model.domain.CardUndoApproval">
        SELECT id,
               request_id AS requestId,
               user_id AS userId,
               create_time AS createTime
        FROM card_undo_approval
        WHERE request_id IN
        <foreach collection="requestIds" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </select>

    <select id="countByRequestId" resultType="int">
        SELECT COUNT(*)
        FROM card_undo_approval
        WHERE request_id = #{requestId}
    </select>

    <insert id="insertIgnore">
        INSERT IGNORE INTO card_undo_approval (request_id, user_id)
        VALUES (#{requestId}, #{userId})
    </insert>

</mapper>
